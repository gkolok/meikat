<%= form_for([@book, @lending]) do |f| %>
  <% if @lending.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lending.errors.count, "error") %> prohibited this lending from being saved:</h2>

      <ul>
      <% @lending.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :kolcsonzo %><br>
    <%= f.text_field :lender %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
